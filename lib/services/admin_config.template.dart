/// Admin configuration
/// 
/// SETUP INSTRUCTIONS:
/// 1. Copy this file to `admin_config.dart` in the same directory
/// 2. Replace the placeholder email with your admin email address(es)
class AdminConfig {
  static final Set<String> adminEmails = {
    'your-admin-email@example.com',
    // Add more admin emails here as needed
  };
}
